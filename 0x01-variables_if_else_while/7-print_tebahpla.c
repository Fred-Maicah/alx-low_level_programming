#include <stdio.h>
/**
 * main - a program that prints the lowercase alphabet in reverse
 *
 * Return: 0 (Success)
 */
int main(void)
{
char k;

k = 'z';
while
	(k >= 'a') {
		putchar(k);
		k--;
	}
putchar('\n');
return (0);
}
